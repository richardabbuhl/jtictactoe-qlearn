<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="backprop" default="all" basedir=".">

    <taskdef resource="net/sf/antcontrib/antcontrib.properties"/>
    <property environment="env"/>
    <property name="src.dir" value="${basedir}"/>
    <property name="out.dir" value="${basedir}/out"/>
    <property name="classes.dir" value="${basedir}/classes"/>

    <!-- Check out dependent modules -->
    <target name="prepare" unless="nocvs">
        <cvs cvsRoot=":pserver:rabbuhl@rabbuhl.demon.nl:/usr/local/cvsroot" package="nanoxml" dest="modules"/>
        <cvs cvsRoot=":pserver:rabbuhl@rabbuhl.demon.nl:/usr/local/cvsroot" package="random" dest="modules"/>
    </target>

    <target name="clean">
        <delete dir="${out.dir}"/>
        <delete dir="${classes.dir}"/>
    </target>

    <target name="compile" depends="clean">
        <mkdir dir="${classes.dir}"/>
        <javac debug="off" destdir="${classes.dir}" srcdir="${src.dir}"
            includes="src/**, modules/nanoxml/src/**, modules/random/src/**"/>
        <!--Copy files to output directory-->
        <!--copy file="xor.xml" tofile="classes/xor.xml"/-->
        <!--copy file="mm2.xml" tofile="classes/mm2.xml"/-->
        <!--copy file="mm.xml" tofile="classes/mm.xml"/-->
    </target>

    <target name="build" depends="compile">
        <mkdir dir="${out.dir}"/>
        <jar jarfile="${out.dir}/jmentor.jar" basedir="${classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="mtrain"/>
            </manifest>
        </jar>
    </target>

    <target name="all" depends="build" description="build all"/>
</project>